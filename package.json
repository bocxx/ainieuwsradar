{
  "name": "ainieuwsradar",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run verify:links && astro build",
    "verify:links": "node scripts/verify-links.mjs",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy",
    "audit:html": "node scripts/check-html-complete.mjs",
    "postbuild": "node scripts/check-html-complete.mjs"
  },
  "dependencies": {
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "wrangler": "^4.133.0"
  }
}
